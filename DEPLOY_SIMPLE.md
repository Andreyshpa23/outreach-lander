# 🚀 Простая инструкция по деплою для новичков

## Что мы будем делать:
Загрузим ваш сайт в интернет, чтобы он работал на вашем домене (например, `yourdomain.com`)

---

## ШАГ 1: Установка Git (если еще не установлен)

### Проверьте, установлен ли Git:
Откройте терминал (на Mac: `Cmd + Space`, введите "Terminal") и выполните:
```bash
git --version
```

Если видите версию (например, `git version 2.x.x`) - Git установлен, переходите к ШАГ 2.

Если видите ошибку - установите Git:

**На Mac:**
1. Откройте https://git-scm.com/download/mac
2. Скачайте и установите Git
3. Или установите через Homebrew: `brew install git`

**На Windows:**
1. Откройте https://git-scm.com/download/win
2. Скачайте и установите Git
3. Во время установки выберите все опции по умолчанию

---

## ШАГ 2: Создание аккаунта на GitHub

1. Откройте https://github.com
2. Нажмите "Sign up" (Зарегистрироваться)
3. Введите:
   - Username (имя пользователя)
   - Email
   - Password (пароль)
4. Подтвердите email (проверьте почту)
5. Выберите бесплатный план (Free)

---

## ШАГ 3: Загрузка кода на GitHub

### 3.1. Откройте терминал в папке проекта

**На Mac:**
1. Откройте Finder
2. Перейдите в папку `outreach-lander`
3. Правой кнопкой мыши → "Services" → "New Terminal at Folder"
   ИЛИ
4. Откройте Terminal и выполните:
   ```bash
   cd ~/outreach-lander
   ```
   (замените путь на ваш реальный путь)

**На Windows:**
1. Откройте папку `outreach-lander` в проводнике
2. В адресной строке введите `cmd` и нажмите Enter
   ИЛИ
3. Правой кнопкой в папке → "Git Bash Here"

### 3.2. Инициализируйте Git в проекте

В терминале выполните:
```bash
git init
```

### 3.3. Добавьте все файлы

```bash
git add .
```

### 3.4. Создайте первый коммит

```bash
git commit -m "First commit"
```

Если видите ошибку про имя и email, выполните сначала:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### 3.5. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите зеленую кнопку "New" (или "+" → "New repository")
3. Введите название: `outreach-lander`
4. Оставьте "Public" (публичный)
5. НЕ ставьте галочки (не создавайте README, .gitignore, license)
6. Нажмите "Create repository"

### 3.6. Загрузите код на GitHub

GitHub покажет инструкции. Выполните в терминале (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
git remote add origin https://github.com/YOUR_USERNAME/outreach-lander.git
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub. Используйте ваш GitHub username и пароль (или Personal Access Token, если включена двухфакторная аутентификация).

**Если не работает push:**
- Создайте Personal Access Token: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Выберите scope `repo`
- Скопируйте токен и используйте его как пароль

---

## ШАГ 4: Деплой на Vercel

### 4.1. Создайте аккаунт на Vercel

1. Откройте https://vercel.com
2. Нажмите "Sign Up"
3. Выберите "Continue with GitHub"
4. Разрешите доступ к GitHub

### 4.2. Импортируйте проект

1. Нажмите "Add New..." → "Project"
2. Найдите репозиторий `outreach-lander`
3. Нажмите "Import"

### 4.3. Настройте проект

Vercel автоматически определит настройки:
- Framework Preset: Next.js ✅
- Root Directory: `./` ✅
- Build Command: `npm run build` ✅
- Output Directory: `.next` ✅

**НЕ меняйте ничего**, просто нажмите "Deploy" внизу.

### 4.4. Добавьте Environment Variables (ВАЖНО!)

После деплоя (через 1-2 минуты):

1. Зайдите в проект на Vercel
2. Откройте "Settings" (Настройки)
3. Выберите "Environment Variables"
4. Добавьте три переменные:

   **Переменная 1:**
   - Name: `AZURE_OPENAI_ENDPOINT`
   - Value: ваш Azure OpenAI endpoint (например, `https://your-resource.openai.azure.com`)
   - Environment: Production, Preview, Development (выберите все три)

   **Переменная 2:**
   - Name: `AZURE_OPENAI_API_KEY`
   - Value: ваш Azure OpenAI API key
   - Environment: Production, Preview, Development (выберите все три)

   **Переменная 3:**
   - Name: `AZURE_OPENAI_DEPLOYMENT`
   - Value: название вашего deployment (например, `gpt-4`)
   - Environment: Production, Preview, Development (выберите все три)

5. Нажмите "Save" для каждой переменной

### 4.5. Передеплойте проект

1. Откройте вкладку "Deployments"
2. Найдите последний деплой
3. Нажмите "..." (три точки) → "Redeploy"
4. Подождите 1-2 минуты

---

## ШАГ 5: Получите URL вашего сайта

После деплоя вы увидите:
- **Production URL**: `outreach-lander.vercel.app` (или похожий)
- Ваш сайт уже работает по этому адресу! 🎉

---

## ШАГ 6: Подключение своего домена (опционально)

Если у вас есть домен (например, `yourdomain.com`):

### 6.1. Добавьте домен в Vercel

1. В проекте Vercel откройте "Settings" → "Domains"
2. Введите ваш домен (например, `yourdomain.com`)
3. Нажмите "Add"

### 6.2. Настройте DNS записи

Vercel покажет, какие DNS записи нужно добавить. Обычно это:

**Вариант 1: A-запись**
- Type: `A`
- Name: `@` (или оставьте пустым)
- Value: IP адрес от Vercel (например, `76.76.21.21`)

**Вариант 2: CNAME-запись**
- Type: `CNAME`
- Name: `@` (или `www`)
- Value: `cname.vercel-dns.com`

### 6.3. Где добавить DNS записи

1. Зайдите в панель управления вашим доменом (где вы покупали домен)
2. Найдите раздел "DNS" или "DNS Management"
3. Добавьте записи, которые показал Vercel
4. Подождите 5-60 минут (DNS обновляется не сразу)

### 6.4. Проверка

Через некоторое время ваш сайт будет доступен по адресу `yourdomain.com`!

---

## 🔄 Как обновлять сайт после изменений

Когда вы измените код:

1. В терминале (в папке проекта):
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```

2. Vercel автоматически задеплоит новую версию (через 1-2 минуты)

---

## ❓ Частые проблемы

### "Git не найден"
- Установите Git (см. ШАГ 1)

### "Permission denied" при push
- Проверьте логин и пароль GitHub
- Или создайте Personal Access Token

### "Environment variables not found"
- Убедитесь, что добавили все три переменные в Vercel
- Передеплойте проект после добавления переменных

### Сайт не работает
- Проверьте, что все Environment Variables добавлены
- Проверьте логи в Vercel: Deployments → выберите деплой → "View Function Logs"

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Скопируйте текст ошибки
2. Проверьте, что все шаги выполнены
3. Посмотрите логи в Vercel

---

## ✅ Чек-лист

- [ ] Git установлен
- [ ] GitHub аккаунт создан
- [ ] Код загружен на GitHub
- [ ] Vercel аккаунт создан
- [ ] Проект импортирован в Vercel
- [ ] Environment Variables добавлены
- [ ] Проект задеплоен
- [ ] Сайт работает по URL от Vercel
- [ ] (Опционально) Домен подключен

---

**Готово!** Ваш сайт теперь в интернете! 🎉

