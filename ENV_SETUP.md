# 🔒 Настройка Environment Variables

## Для ограничения использования токенов

Добавьте в Vercel (Settings → Environment Variables):

### Переменная: `DAILY_REQUEST_LIMIT`

**Описание:** Максимальное количество запросов в день

**Примеры значений:**
- `50` - максимум 50 запросов в день
- `100` - максимум 100 запросов в день
- `200` - максимум 200 запросов в день
- `0` - без ограничений (по умолчанию)

**Как работает:**
- Считаются запросы к API (`/api/generate` и `/api/collect-info`)
- Счетчик сбрасывается каждый день в 00:00 UTC
- При превышении лимита пользователь видит сообщение: "Daily limit reached. Please try again tomorrow."

**Рекомендации:**
- Для тестирования: `10-20` запросов
- Для продакшена: `50-200` запросов (в зависимости от вашего бюджета)
- Один полный цикл (сбор информации + генерация) = примерно 2-5 запросов

---

## MinIO (S3-совместимое хранилище)

Используется для: demo-import (X-Fast-Creation → DEMO_IMPORT_SDR cookie), leadgen CSV.

- **`MINIO_ENDPOINT`** — адрес API MinIO (порт **9000**, не 9001). Пример: `http://92.118.114.94:9000`
- **`MINIO_BUCKET`** — имя бакета. Пример: `demo-salestrigger`
- **`MINIO_ACCESS_KEY`** — Access Key из MinIO Console (Identity → Service Accounts / Access Keys)
- **`MINIO_SECRET_KEY`** — Secret Key

UI MinIO: порт 9001. Для приложения нужен API: порт 9000.

### Где что лежит в бакете

| Путь в бакете | Что там | Кто пишет |
|---------------|---------|-----------|
| **`demo-imports/`** | JSON-файлы (`uuid.json`) — продукт, сегменты, LinkedIn URL лидов | POST /api/demo-import и leadgen worker (когда есть лиды + minio_payload) |
| **`leadgen-csv/`** | CSV-файлы выгрузки лидов Apollo | Leadgen worker (всегда при успешном сборе лидов) |

Если запускали **leadgen** и ждёте CSV — смотрите папку **leadgen-csv/** в том же бакете, не только demo-imports.

---

## Apollo (сбор лидов)

Для leadgen (POST /api/leadgen) нужен master API key из [Apollo API](https://docs.apollo.io/reference/people-api-search):

- **`APOLLO_API_KEY`** — Master API Key из Apollo (Settings → API Keys)

---

## Существующие переменные (уже настроены):

- `AZURE_OPENAI_ENDPOINT` - ваш Azure OpenAI endpoint
- `AZURE_OPENAI_API_KEY` - ваш Azure OpenAI API key  
- `AZURE_OPENAI_DEPLOYMENT` - название вашего deployment

---

## Деплой на Vercel — все переменные

Чтобы на Vercel работали demo-import (MinIO), leadgen (Apollo) и основной флоу:

| Переменная | Назначение |
|------------|------------|
| `AZURE_OPENAI_ENDPOINT` | Azure OpenAI |
| `AZURE_OPENAI_API_KEY` | Azure OpenAI |
| `AZURE_OPENAI_DEPLOYMENT` | Azure OpenAI |
| `AZURE_OPENAI_API_VERSION` | (опционально) |
| `DAILY_REQUEST_LIMIT` | Лимит запросов в день |
| `MINIO_ENDPOINT` | MinIO API (порт 9000) |
| `MINIO_BUCKET` | Имя бакета |
| `MINIO_ACCESS_KEY` | MinIO |
| `MINIO_SECRET_KEY` | MinIO |
| `APOLLO_API_KEY` | Master API key Apollo (сбор лидов) |

Leadgen (POST /api/leadgen/run) может выполняться до 60 сек — на Vercel Pro лимит 60s по умолчанию.

---

## Как добавить переменную в Vercel:

1. Зайдите на https://vercel.com
2. Откройте ваш проект
3. Settings → Environment Variables
4. Добавьте:
   - Key: `DAILY_REQUEST_LIMIT`
   - Value: `50` (или другое число)
   - Environment: выберите Production, Preview, Development
5. Нажмите "Save"
6. Передеплойте проект (Deployments → ... → Redeploy)

---

## Проверка работы:

После добавления переменной:
1. Сделайте несколько запросов
2. Когда лимит будет достигнут, пользователь увидит сообщение об ошибке
3. На следующий день счетчик сбросится автоматически


