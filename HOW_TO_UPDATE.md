# 📝 Как обновлять код и деплоить изменения

## Простой процесс (3 шага):

### 1️⃣ Я редактирую код
Вы говорите мне, что нужно изменить, и я редактирую файлы на вашем компьютере.

### 2️⃣ Вы загружаете изменения на GitHub
Выполните в терминале (в папке проекта):

```bash
cd ~/outreach-lander
git add .
git commit -m "Описание изменений"
git push
```

### 3️⃣ Vercel автоматически задеплоит
Через 1-2 минуты Vercel автоматически обновит ваш сайт!

---

## 📋 Пошаговая инструкция:

### Шаг 1: Скажите мне, что изменить
Например:
- "Измени заголовок на 'Мой новый заголовок'"
- "Добавь кнопку"
- "Исправь ошибку в чате"

Я отредактирую файлы.

### Шаг 2: Откройте терминал
**На Mac:**
- Нажмите `Cmd + Space`
- Введите "Terminal"
- Нажмите Enter

**Или:**
- Откройте Finder
- Перейдите в папку `outreach-lander`
- Правой кнопкой → "Services" → "New Terminal at Folder"

### Шаг 3: Выполните команды

Скопируйте и выполните по очереди:

```bash
cd ~/outreach-lander
```

(Если терминал уже в папке проекта, эту команду можно пропустить)

```bash
git add .
```

```bash
git commit -m "Обновление: опишите что изменили"
```

Например:
- `git commit -m "Изменил заголовок"`
- `git commit -m "Добавил новую кнопку"`
- `git commit -m "Исправил баг в чате"`

```bash
git push
```

Если попросит пароль - введите ваш GitHub пароль (или Personal Access Token).

### Шаг 4: Проверьте деплой

1. Зайдите на https://vercel.com
2. Откройте ваш проект
3. Во вкладке "Deployments" увидите новый деплой
4. Через 1-2 минуты сайт обновится!

---

## 🎯 Пример полного процесса:

**Вы:** "Измени заголовок 'Your AI Sales Agent' на 'AI Sales Agent Pro'"

**Я:** Редактирую файл `src/app/page.tsx`

**Вы:** Выполняете в терминале:
```bash
cd ~/outreach-lander
git add .
git commit -m "Изменил заголовок на AI Sales Agent Pro"
git push
```

**Результат:** Через 1-2 минуты сайт обновится с новым заголовком!

---

## 💡 Полезные команды:

### Посмотреть, что изменилось:
```bash
git status
```

### Отменить изменения (если что-то пошло не так):
```bash
git checkout .
```

### Посмотреть историю изменений:
```bash
git log
```

---

## ⚠️ Важно:

1. **Всегда выполняйте команды в папке проекта** (`outreach-lander`)
2. **Не редактируйте файлы напрямую на GitHub** - я редактирую локально, вы пушите
3. **Если видите ошибку** - скопируйте текст ошибки и покажите мне

---

## 🚀 Быстрый способ (скрипт):

Можете создать файл `update.sh` в папке проекта:

```bash
#!/bin/bash
git add .
git commit -m "$1"
git push
```

Тогда можно просто выполнять:
```bash
./update.sh "Описание изменений"
```

Но для начала используйте команды по отдельности - так понятнее.

---

**Готово!** Теперь вы можете легко обновлять сайт! 🎉


